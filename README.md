# Restaurant-Reservation-and-Order-System
A Restaurant Management System built with C# WinForms and SQL Server. Features admin login, table reservations, food order tracking with auto bill calculation, and a live sales dashboard with charts. Uses stored procedures, triggers, and functions for core database logic.
